<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.1</TargetFramework>
        <RootNamespace>Depra.Loading</RootNamespace>
        <AssemblyName>Depra.Loading</AssemblyName>
        <LangVersion>9</LangVersion>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <Optimize>true</Optimize>
      <DebugType>embedded</DebugType>
    </PropertyGroup>

    <ItemGroup>
      <Reference Include="Depra.Threading">
        <HintPath>..\..\Depra.Threading\Depra.Threading\bin\Release\netstandard2.1\Depra.Threading.dll</HintPath>
      </Reference>
    </ItemGroup>

</Project>
